[project]
name = "aiagent"
version = "1.0.0"
description = "AI Agent using free tokens to get, write, and run Python files, written in python"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "openai==2.44.0",
    "python-dotenv==1.1.0",
]
